\documentclass[11pt, a4paper]{ltjsarticle}

\usepackage{amsmath, amssymb, amsthm, mathtools}
\usepackage{bm}
\usepackage{physics} 
\usepackage{graphicx}
\usepackage[unicode, hidelinks]{hyperref}

\title{一端を固定された紐の落下挙動の解析：補足資料}
\author{黒瀬 諭一朗 \\ 埼玉県立大宮高等学校物理部}
\date{\today}

\begin{document}

\maketitle

\section{はじめに}
本稿は，「一端を固定された紐の落下挙動の解析」の要旨において，紙面の都合上割愛せざるを得なかった詳細な計算過程をまとめた補足資料である．

本研究では，紐を「長さ$\Delta l = l/N$の剛体棒と質量$\Delta m = m/N$質点からなる$N$重振り子」として定式化し，$N\rightarrow\infty$の極限をとることで解析を行った．
要旨に記載した通り，この解析過程では初角加速度に関する連立方程式から積分方程式への移行，そしてディラックのデルタ関数の出現という重要な数理的ステップが存在する．

そこで本稿では，これらの飛躍を埋めるため，詳細な計算過程を解説する．
第2章では，系の運動エネルギーとポテンシャル・エネルギーを導出し，ラグランジアンを構成する過程を示す．
第3章では，微小時間の仮定のもとでテイラー展開を用いて角度を近似し，運動方程式を導出する過程を示す．
第4章では，得られた方程式に対して$N \rightarrow\infty$の極限を取り，連続体としての振る舞いを記述する積分方程式へと移行する過程を示す．

\section{ラグランジアンの導出}
紐の長さを$l$，質量を$m$とする．紐を$N$等分し，長さ$\Delta l = l/N$の剛体棒と，
質量$\Delta m = m/N$の質点が連なった$N$重振り子を考える．
初期状態において，振り子は鉛直線と角度$\theta$をなす直線上で静止しているとする．
固定端を原点とし，地面と平行に$x$軸，鉛直下向きに$y$軸をとる．
時刻$t = 0$において静止状態から自由落下させた．

時刻$t$における$j$番目の質点が鉛直線となす角度を$\varphi_j$とすると，
時刻$t$における$i$番目の質点の座標$\left(x_i, y_i\right)$は，次のように表される．
\begin{equation*}
  x_i = \Delta l \sum_{j = 1}^{i} \sin \varphi_j, \quad
  y_i = \Delta l \sum_{j = 1}^{i} \cos \varphi_j.
\end{equation*}

系の運動エネルギー$T$は，
\begin{equation*}
  T = \frac{1}{2} \varDelta m \sum_{i = 1}^{N} \left(\dot{x_i}^2 + \dot{y_i}^2\right)
\end{equation*}
と表される．ここで，
\begin{equation*}
  \dot{x_i}^2 + \dot{y_i}^2
  = \left(\Delta l \sum_{j = 1}^{i} \dot{\varphi_j} \cos \varphi_j\right)^2
  + \left(\Delta l \sum_{j = 1}^{i} \dot{\varphi_j} \sin \varphi_j\right)^2.
\end{equation*}
これを展開して整理すると次のようになる．
\begin{equation*}
  \dot{x_i}^2 + \dot{y_i}^2
  = {\Delta l}^2 \sum_{j = 1}^{i} \sum_{k = 1}^{i}
    \dot{\varphi_j} \dot{\varphi_k} \cos \left(\varphi_j - \varphi_k\right).
\end{equation*}
したがって，
\begin{equation*}
  T = \frac{1}{2} \Delta m {\Delta l}^2
      \sum_{i = 1}^{N} \sum_{j = 1}^{i} \sum_{k = 1}^{i}
      \dot{\varphi_j} \dot{\varphi_k} \cos \left(\varphi_j - \varphi_k\right).
\end{equation*}
これは和の順序を入れ替えることで次のように変形できる．
\begin{equation}
  T = \frac{1}{2} \Delta m {\Delta l}^2
      \sum_{j = 1}^{N} \sum_{k = 1}^{N} \left(N - \max \left(j, k\right) + 1\right)
      \dot{\varphi_j} \dot{\varphi_k} \cos \left(\varphi_j - \varphi_k\right).
\end{equation}

一方，系のポテンシャル・エネルギー$U$は，
\begin{equation*}
  U = - \Delta m g \sum_{i = 1}^{N} y_i
\end{equation*}
すなわち，
\begin{equation*}
  U = - \Delta m g \Delta l \sum_{i = 1}^{N} \sum_{j = 1}^{i} \cos \varphi_j
\end{equation*}
と表される．これは和の順序を入れ替えることで次のように変形できる．
\begin{equation}
  U = - \Delta m g \Delta l \sum_{j = 1}^{N} \left(N - j + 1\right)
      \cos \varphi_j.
\end{equation}

(1)，(2)によりラグランジアン$L = T - U$が構成され，ラグランジュ方程式
\begin{equation}
  \frac{\mathrm{d}}{\mathrm{d}t} \frac{\partial L}{\partial \dot{\varphi_k}}
  - \frac{\partial L}{\partial \varphi_k} = 0
  \quad \left(k = 1, 2, \dots, N\right)
\end{equation}
が定まる．

\section{運動方程式の導出}
本研究で考えるのは落下挙動であるため，$t$は微小であるとする．

初期条件により，$t = 0$において角速度は$0$，角度は$\theta$である．
したがって，$t = 0$における角加速度を$\alpha_k$とおくと，$t$が微小のとき，
$\varphi_k$は$t = 0$まわりでテイラー展開することで次のように表される．
\begin{equation}
  \varphi_k = \theta + \frac{\alpha_k}{2}t^2.
\end{equation}

$\alpha_k$を得るために，(3)に$t = 0$を代入した式
\begin{equation}
  \left.\frac{\mathrm{d}}{\mathrm{d}t} \frac{\partial L}{\partial \dot{\varphi_k}}\right|_{t = 0}
  - \left.\frac{\partial L}{\partial \varphi_k}\right|_{t = 0} = 0
\end{equation}
を計算する．

(5)の第一項を求める．$L$を$\dot{\varphi_k}$で偏微分すると，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \frac{\partial}{\partial \dot{\varphi_k}}
    \frac{1}{2} \Delta m {\Delta l}^2
    \sum_{a = 1}^{N} \sum_{b = 1}^{N} \left(N - \max \left(a, b\right) + 1\right)
    \dot{\varphi_a} \dot{\varphi_b} \cos \left(\varphi_a - \varphi_b\right)
\end{equation*}
であり，
\begin{equation*}
  C_{ab} = \frac{1}{2} \Delta m {\Delta l}^2 \left(N - \max \left(a, b\right) + 1\right)
           \cos \left(\varphi_a - \varphi_b\right)
\end{equation*}
とおくと，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \sum_{a = 1}^{N} \sum_{b = 1}^{N} C_{ab}
    \frac{\partial}{\partial \dot{\varphi_k}} \dot{\varphi_a} \dot{\varphi_b}.
\end{equation*}
積の導関数の公式により，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \sum_{a = 1}^{N} \sum_{b = 1}^{N} C_{ab}
    \left(\frac{\partial \dot{\varphi_a}}{\partial \dot{\varphi_k}} \dot{\varphi_b}
        + \frac{\partial \dot{\varphi_b}}{\partial \dot{\varphi_k}} \dot{\varphi_a}\right).
\end{equation*}
すなわち，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \sum_{a = 1}^{N} \sum_{b = 1}^{N} C_{ab}
    \frac{\partial \dot{\varphi_a}}{\partial \dot{\varphi_k}} \dot{\varphi_b}
  + \sum_{a = 1}^{N} \sum_{b = 1}^{N} C_{ab}
    \frac{\partial \dot{\varphi_b}}{\partial \dot{\varphi_k}} \dot{\varphi_a}.
\end{equation*}
$\partial \dot{\varphi_a}/\partial \dot{\varphi_k}$は，
$a = k$のとき$1$，$a \neq k$のとき$0$であり，$b$についても同様である．したがって，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \sum_{b = 1}^{N} C_{kb} \dot{\varphi_b} + \sum_{a = 1}^{N} C_{ak} \dot{\varphi_a}.
\end{equation*}
$a$，$b$を$j$に置き換えて整理すると，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}} = 2 \sum_{j = 1}^{N} C_{jk} \dot{\varphi_j}.
\end{equation*}
すなわち，
\begin{equation*}
  \frac{\partial L}{\partial \dot{\varphi_k}}
  = \Delta m {\Delta l}^2 \sum_{j = 1}^{N}
    \left(N - \max \left(j, k\right) + 1\right) \dot{\varphi_j}
    \cos \left(\varphi_j - \varphi_k\right).
\end{equation*}
これを$t$について微分し，$t = 0$を代入すると，初期条件より，
\begin{equation}
  \left.\frac{\mathrm{d}}{\mathrm{d}t} \frac{\partial L}{\partial \dot{\varphi_k}}\right|_{t = 0}
  = \Delta m {\Delta l}^2 \sum_{j = 1}^{N}
    \left(N - \max \left(j, k\right) + 1\right) \alpha_j.
\end{equation}

(5)の第二項を求める．$L$を$\varphi_k$で偏微分し，$t = 0$を代入すると，初期条件より，
\begin{equation}
  \left.\frac{\partial L}{\partial \varphi_k}\right|_{t = 0}
  = - \Delta m g \Delta l \left(N - k + 1\right) \sin \theta.
\end{equation}

(6)，(7)より，(5)は，
\begin{equation*}
  \Delta m {\Delta l}^2 \sum_{j = 1}^{N}
  \left(N - \max \left(j, k\right) + 1\right) \alpha_j
  + \Delta m g \Delta l \left(N - k + 1\right) \sin \theta = 0.
\end{equation*}
両辺を$\Delta m$で割って整理すると，$\alpha_j$に関する次の方程式が得られる．
\begin{equation}
  \sum_{j = 1}^{N} \left(N \Delta l
  - \max \left(j \Delta l, k \Delta l\right) + \Delta l\right) \alpha_j
  \Delta l + g \left(N \Delta l - k \Delta l + \Delta l\right) \sin \theta = 0.
\end{equation}

\section{$N\rightarrow\infty$の極限}
式(8)における，$N \rightarrow \infty$の極限を考える．
固定端からの距離を$s' = j \Delta l$，$s = k \Delta l$と対応させる．
$N \rightarrow \infty$において和は積分となり，
角加速度$\alpha_j$は$s'$の関数$\alpha\left(s'\right)$となるため，
式(8)は次の積分方程式となる．
\begin{equation}
  \int_{0}^{l} \left(l - \max \left(s', s\right)\right)
  \alpha\left(s'\right) \,\mathrm{d}s'
  + g \left(l - s\right) \sin \theta = 0. 
\end{equation}
積分区間を分割して整理すると，
\begin{equation*}
  \left(l - s\right) \int_{0}^{s} \alpha\left(s'\right) \,\mathrm{d}s'
  + \int_{s}^{l} \left(l - s'\right) \alpha\left(s'\right) \,\mathrm{d}s'
  + g \left(l - s\right) \sin \theta = 0.
\end{equation*}
両辺を$s$について微分すると，
\begin{equation*}
  \left( - \int_{0}^{s} \alpha\left(s'\right) \,\mathrm{d}s'
  + \left(l - s\right) \alpha\left(s\right) \right)
  - \left(l - s\right) \alpha\left(s\right)
  - g \sin \theta = 0.
\end{equation*}
すなわち，
\begin{equation}
  \int_{0}^{s} \alpha\left(s'\right) \,\mathrm{d}s' = -g \sin \theta.
\end{equation}
$s > 0$において，両辺を$s$について微分すると，
\begin{equation}
  \alpha\left(s\right) = 0 \quad \left(s > 0\right).
\end{equation}
(10)，(11)を満たすためには，ディラックのデルタ関数$\delta \left(s\right)$が必要となる．
したがって，(9)の解は次のように表される．
\begin{equation}
  \alpha\left(s\right) = -g \sin \theta \cdot \delta \left(s\right).
\end{equation}

(4)，(12)より，固定端からの距離が$s$である点における紐の接線と鉛直線がなす角度
$\varphi \left(s\right)$は，次のように表される．
\begin{equation}
  \varphi\left(s\right) = \theta - \frac{1}{2} g t^2 \sin \theta \cdot \delta \left(s\right).
\end{equation}

\end{document}
